namespace Management.ProjectHelper
{
    class Try
    {               /*DashBoard fs = new DashBoard();
                    
                    SqlConnection conn;
                    conn = new SqlConnection("Data Source=DIRICHLET;Initial Catalog=Management;Integrated Security=True");
                    conn.Open();
                    string sql2 = string.Format(@"select username,authority form userinfo where account='{0}'and password ='{1}'",account, password);
                    SqlDataAdapter myDataAdapter = new SqlDataAdapter(sql2, conn);
                    DataSet myDataSet = new DataSet();      // 创建DataSet
                    
                    myDataAdapter.Fill(myDataSet, "userinfo");                
                    DataSet MyDataSet = new DataSet();
                    DataTable Mytable = MyDataSet.Tables["userinfo"];
                    fs.username = Mytable.Columns[0].ToString();
                    fs.authority = Mytable.Columns[1].ToString();
                    fs.account = account;
                    conn.Close();
                    fs.Show();
                    button1.Text = "登录";*/
    }
}
